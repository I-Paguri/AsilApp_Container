<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="30dp" />
    <solid android:color="@color/pastel_red" />
    <stroke
        android:color="@color/pastel_red"
        android:width="1dp" />
</shape>
